---
title: "Part II — Personal Practice (executive summary)"
kind: part-summary
draft: true
part_source: 2
description: "Covers Ch 5-8 in 2 pages. Shows the reader's journey from L2 (Configured) to L3 (Practiced). Draft — the Part II chapters are not yet ported; this summary is the macro arc."
last_updated: "2026-06-18"
visual_intent: "Wide synthesis. Reader-journey framing: L2 → L3 progression. Bullet-dense; minimal prose. Draft until the Part II chapters land; the per-chapter supplements are listed under Drafts on the supplements index."
---

import KeyIdea from '@brandon_m_behring/book-scaffold-astro/components/KeyIdea.astro';
import MarginNote from '@brandon_m_behring/book-scaffold-astro/components/MarginNote.astro';

> **You are at L2 (Configured)** — Part I gave you a working CLAUDE.md, deny rules in `settings.json`, a repeatable session loop, and a prompting vocabulary that gets reasonable first-try results. Sessions don't drift; conventions are picked up reliably. By the end of Part II, you'll be at **L3 (Practiced)** — your sessions stay on the rails for hours, you catch sycophantic answers before acting on them, the code Claude produces passes a verification gate before you call it done, and you reach for the right extension (command vs skill vs hook vs MCP) without thinking.

<KeyIdea>
Part II is where the gains compound. Part I gave you the configuration that makes Claude reliable per-session; Part II gives you the *practice* that makes Claude reliable across the long tail — long sessions, hard debugging, ambiguous prompts, the extensions you'll grow into. Each chapter installs one durable habit.
</KeyIdea>

> **Draft.** The Part II chapters (Ch 5–8) are not yet ported. Their supplements exist as drafts — browse them under **Drafts** on the [supplements index](/supplements). Once the chapters land, each will surface its own supplements in a **Go deeper** block (as Part I's chapters do) and this summary will be finalized.

## The arc

```
Ch 5 — Context ─→ Ch 6 — Thinking ─→ Ch 7 — Verification ─→ Ch 8 — Extending
     │                  │                    │                       │
"sessions stop      "Claude as         "tests pass,            "reach for the
 falling over"        partner, not        does it actually        lightest tool
                      oracle"             solve it?"              that does the job"
     │                  │                    │                       │
     L2                 L2.5                L2.5+                   L3
```

## Chapter summaries

### Ch 5 — Context as Currency

**Core claim**: every tool call costs tokens, and a session's working memory degrades as it fills. `/clear`, `/compact`, `/rewind`, `/btw` are the four moves; the two-failure rule is the trigger that gets you to use them. Session naming + `CURRENT_WORK.md` extend useful sessions across days.

### Ch 6 — Thinking Together

**Core claim**: delegating to Claude as an oracle produces sycophantic mush. Collaborating with Claude as a thinking partner — via hypothesis debugging, pre-mortem, Feynman, and the interview pattern — surfaces real reasoning. The interview pattern is the third frame between "delegate" and "collaborate" and the one most teams underuse. ADRs preserve the decisions that come out of these sessions.

### Ch 7 — Testing & Verification

**Core claim**: the difference between "Claude said it works" and "it actually works" is verification. Six layers (type-check / lint / unit / integration / e2e / manual) plus a 7th (domain correctness — only humans can do it). Phase-appropriate standards: prototype skips layers 4-7; production runs all 7. Verification criteria belong in `CLAUDE.md`; enforcement belongs in hooks.

### Ch 8 — Extending Claude

**Core claim**: there is a decision framework before there is any extension type. Reach for the lightest extension that does the job; build new only when consuming fails. Order of escalation: custom slash command → skill → hook → MCP server. Hooks are for enforcement, not feature-add. MCP is for genuine third-party integration, not as a default. Designing MCP servers and Agent SDK hooks lives in the Architect's Reference; this chapter teaches consumption.

## What you'll have built by end of Part II

- A **session-management vocabulary** (`/clear` / `/compact` / `/rewind` / `/btw`) you reach for automatically when the two-failure rule fires
- A **`CURRENT_WORK.md`** that survives across sessions for at least one ongoing piece of work
- A **collaboration repertoire** (hypothesis debugging, pre-mortem, Feynman, interview) — at least one of each invoked in a real situation
- A **`CLAUDE.md`** that names verification criteria (no "handle errors gracefully" mush)
- At least one **hook** in `.claude/settings.json` that enforces something you care about (a `Stop` hook that runs `verify.sh`, or a `PreToolUse` that blocks `rm -rf`)
- At least one **custom skill** in `.claude/skills/` that packages a recurring procedure
- At least one **ADR** in `.claude/decisions/` capturing a non-obvious choice

That's enough to make Claude reliable across the long tail of work, not just per-session. Parts III–IV build on this foundation: hub-and-spoke configuration at scale, subagents for parallelism, team governance, enterprise deployment.

## What's NOT in Part II (deferred to later Parts)

- **Hub-and-spoke CLAUDE.md** for monorepos — Ch 9, Part III
- **Subagent orchestration** — Ch 10, Part III
- **Project lifecycle** (long-running projects, milestones, retrospective) — Ch 11, Part III
- **Headless automation + scheduled agents** — Ch 12, Part III
- **Antipatterns deep-dive** — Ch 13, Part III
- **Team governance + sharing CLAUDE.md** — Ch 14, Part IV
- **Enterprise deployment** (auth, compliance, FedRAMP) — Ch 15, Part IV
- **MCP server design + Agent SDK hook authoring** — Architect's Reference (Volume 2)

The sequencing is deliberate: Part II's *practice* is what makes Part III's *scale* not break.
