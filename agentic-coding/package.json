{
  "name": "agentic-coding",
  "description": "Agentic Coding: Principles and Practices — cross-tool (Claude Code · Gemini CLI · Codex CLI) 'using coding agents' book. Folded into claude-books from book-template-astro (v3) 2026-06-16.",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "predev": "npm run build:labels --if-present && npm run sync:series",
    "prebuild": "npm run build:labels --if-present && npm run sync:series && npm run validate --if-present",
    "sync:series": "node ../scripts/sync-series.mjs agentic-coding",
    "build:labels": "book-scaffold build-labels",
    "build:figures": "book-scaffold build-figures",
    "build:notebooks": "book-scaffold render-notebooks",
    "validate": "BOOK_PROFILE=tools book-scaffold validate",
    "dev": "BOOK_PROFILE=tools astro dev",
    "build": "BOOK_PROFILE=tools astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.3",
    "@astrojs/preact": "^5.1.1",
    "@brandon_m_behring/book-scaffold-astro": "^4.25.0",
    "astro": "^6.3.7",
    "preact": "^10.29.1"
  }
}
