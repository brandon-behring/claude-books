# Session handoff — Wave 5 COMPLETE (Agents & Orchestration D1 spine + 17-wide merge + bridge)

**Date:** 2026-05-27 · **Branch:** `research-program-v0` (NOT merged to `main`) · **Resume at:** Wave 6 (review gate pending parent commit).

Supersedes `2026-05-27_wave4-complete-handoff.md` (which resumed at Wave 5 — now done).

---

## Resume in one line

**Wave 5 is complete and bridged.** Four strict-live dossiers (`harness_frame`, `harness_buildvsbuy`, `sub_agents`, `multi_agent`) — the **D1 Agents & Orchestration architecture spine** — are built, audited, exported, and **bridged into the repo backbone**; the orphan `capabilities_scaling` dossier was **bridged as a new positioned node** (`agent-capabilities`). The cross-project KG-compose merge **proves D1/D2/D5 at 17-wide** (1120 raw → 1092 composed, **0 claim/evidence ID collisions** asserted at merge). 17 topics are now strict-live across five bridged waves. The bridge edits are staged but **NOT committed** (parent commits after review). **Next: Wave 6** (Production Operations — D5) using the now-sixteen-times-proven per-dossier recipe.

## What's done (this session)

1. **Dossier `harness_frame`** (`~/Claude/research_agent_harness_frame/`, node `harness-frame`, stub→strict-live) — 10 sources, 20 atoms, 4 families (frame_definition · inner_outer_loop · components_taxonomy · provenance). `verify_citations` **20/20**. CoVe r1: **0/0/0 clean**. `synthesis.md` (5 `syn_harness_frame_*`). The **root of the D1 architecture spine**: Agent = Model + Harness; inner (reason→act→observe) vs outer loop; the 3-layer context/environment/assembly model; the components taxonomy. **The provenance family is the methodology highlight** — it anchors the dated vocabulary-crystallization arc (Karpathy "Decade of Agents" 2025-06-17 → Anthropic adopts "harness" 2025-11-26 → Hashimoto coins "harness engineering" 2026-02-05 → Trivedy "Agent = Model + Harness" 2026-03-10) as **dated-provenance-NOT-definition** (mixed T1/T2/T3, escalate-don't-launder: the load-bearing definition stays the T1 frame; the T3 transcript-mirror/practitioner posts are admissible only as dated history). slug `research_agent_harness_frame`.
2. **Dossier `harness_buildvsbuy`** (`~/Claude/research_agent_harness_buildvsbuy/`, node `harness-buildvsbuy`, stub→strict-live) — 7 sources, 14 atoms, 3 families (decision_criteria · framework_landscape · …). `verify_citations` **14/14**. CoVe r1: **1 CORRECT** (managed-agents authorship). `synthesis.md` (5 `syn_harness_buildvsbuy_*`). When to build a custom harness vs configure/wrap an existing one (Agent SDK, LangGraph, managed agents); the decision criteria; the framework landscape; the middle path. slug `research_agent_harness_buildvsbuy`.
3. **Dossier `sub_agents`** (`~/Claude/research_agent_sub_agents/`, node `agent-subagents`, cache→strict-live) — 7 sources, 18 atoms, 3 families (isolation_mechanism · …). `verify_citations` **18/18**. CoVe r1: **1 FLAG** — the `claude.com/blog` SPA shows **live↔cache drift** (a client-rendered SPA whose live DOM diverges from the cached snapshot); **the cache is the source of truth** (anchored verbatim against the cached blob). `synthesis.md` (5 `syn_sub_agents_*`). Sub-agents as a context-isolation mechanism (fresh context, inherits nothing, forked sub-agents); Planner/Generator/Evaluator; isolation-not-capability. slug `research_agent_sub_agents`.
4. **Dossier `multi_agent`** (`~/Claude/research_agent_multi_agent/`, node `agent-multipatterns`, cache→strict-live) — 6 sources, 15 atoms, 3 families (topologies · verification · anti_patterns). `verify_citations` **15/15**. CoVe r1: **1 CORRECT** (multi-agent-research authorship). `synthesis.md` (5 `syn_multi_agent_*`). Orchestrator-worker topologies, when-not-to, the verification sub-agent, the role-decomposition anti-pattern. **Payload — the tension stays UNFLATTENED:** the dossier carries **Anthropic's orchestrator-worker** ("multi-agent works for breadth-first parallelizable research") and **Cognition's "Don't Build Multi-Agents"** (context-sharing / decision-conflict argues against) as **two anchored framings, not reconciled** — the disagreement is itself the finding. slug `research_agent_multi_agent`.
5. **Orphan `capabilities_scaling` bridged** (`~/Claude/research_agent_capabilities_scaling/`, **new node `agent-capabilities`**) — the complete v3 dossier built **May-20** (pre-wave), re-verified at toolkit **v2.5.0**: 11 sources, 14 atoms, citation-clean. Bridged as a **positioned** (not active-frontier) node — orthogonal **background** on agent/model capability (scaling behaviour, emergence-vs-measurement, capability trajectory): the **Model half of Agent = Model + Harness**, *not* a how-to topic. This retires the Wave-4 open item ("decide whether to bridge or formally retire the orphan") by bridging it. Single-track dossier (no track-split files; has a `cache/` subdir + `pre_selection_manifest.yml`). slug `research_agent_capabilities_scaling`.
6. **17-wide cross-project KG-compose** — `WAVE=wave5 MERGE_DATE=2026-05-27 ~/Claude/_merge_projects.py` across all 17 projects: **1120 raw → 1092 composed** (28 shared nodes collapsed); **141 distinct sources, 14 shared, 5 shared caches**; HumanLayer same-as applied (1); **0 claim/evidence ID collisions** (disjoint-prefix discipline held at 17-wide — **asserted at merge**). Output: `~/Claude/research-kb/composed/wave5_*` (the 12-wide `wave4_*` and earlier snapshots **preserved byte-identical**). **The richest cross-dossier overlap yet** — the D1 corpus shares its core primaries widely: **building-effective-agents** ↔ `harness_frame` (frame_definition) + `harness_buildvsbuy` (decision_criteria) + `multi_agent` (verification); **multi-agent-research** ↔ `sub_agents` (isolation_mechanism) + `multi_agent` (topologies); plus Agent-SDK / LangGraph / effective-context-engineering / effective-harnesses all shared across the harness pair and the context dossiers. Every shared source resolved by `primary_url`+`sha256`, all `claim_family` framings preserved (D2/D6). Shared-source count rose 7→14.
7. **Bridge staged in-repo** (NOT committed — parent commits after review): `taxonomy.graph.json` (4 nodes flipped to `ingested`/`strict-live` + `project_slug`, +1 new `agent-capabilities` node, meta `node_count` 44→45 + Wave-5 note; **valid JSON confirmed**), `trajectories.md` (4 rows → DOSSIER COMPLETE + 1 new `agent-capabilities` row), `content-map.md` (Wave-5 note + 17-project KG pointer + the unflattened-tension payload), `topic-backlog.yml` (`agent-capabilities-emergence-scaling` → covered; `agentic-engineering-vibe-coding` → partial; 4 new candidates; clarifying notes on `twelve-factor-agents` + `deep-agents-pattern` that the parent went strict-live but the sub-topic stayed out of scope).

## Methodology notes (this wave)

- **The provenance-as-dated-history pattern (`harness_frame`).** A whole claim family (`provenance`) anchors *who coined/popularized a term, and when* — never used to assert a definition. Mixed-tier (T1 Anthropic-adoption + T2 framework-vendor + T3 transcript-mirror/practitioner), admissible **only** because the claim is dated provenance, not a load-bearing definition; the definition stays the T1 frame. Reusable for any topic whose *vocabulary* is still crystallizing (logged for the toolkit burn-in).
- **Tension-preservation (`multi_agent`).** When two T1/T2 primaries genuinely disagree (Anthropic orchestrator-worker vs Cognition don't-build-multi-agents), anchor **both** and present the disagreement as the finding — do **not** synthesize a false convergence. This is a `<Tag>` *tension* in the book, not a `convergence`.
- **The SPA live↔cache drift flag (`sub_agents`).** `claude.com/blog` is a client-rendered SPA; its live DOM can diverge from the cached snapshot. Convention: **the cache is the source of truth** for the anchored excerpt (the CoVe FLAG records the drift, not a correction). Recheck if/when the SPA content stabilizes — see open items.

## Review-gate assessment

- **The per-topic pipeline is robust at sixteen-times scale** (pilot + 16 dossiers). The 8-step recipe ran to all-gates-green for all four Wave-5 dossiers; CoVe round-1 was clean-or-single-touch on every one (0/0/0, 1 CORRECT, 1 FLAG, 1 CORRECT).
- **The cross-project merge holds at 17-wide WITH the densest overlap so far.** The D1 corpus is *not* disjoint — its core primaries (building-effective-agents, multi-agent-research, Agent-SDK, LangGraph) are shared across three-to-five dossiers each, and the merge resolved all of them (0 collisions, every framing kept). Shared sources rose 7→14; this is a stronger D2 proof than any prior wave.
- **The orphan is resolved.** `capabilities_scaling` is bridged as a positioned background node rather than left dangling or retired — the Wave-4 open item is closed.
- **Escalate-don't-launder held under provenance pressure.** The harness_frame search surfaced a clean-looking T3 harness definition; tier discipline rejected it (duplicative-of-T1 for definitions; admissible only as T3 provenance). Implausible future-dated arXiv ids were treated as non-credible and not fetched.

## Wave 6 — Production Operations (D5), running straight through

The remaining near-term cluster (deferred since Wave 4's review gate). Pick order is the editorial depth-gate (D3), not a momentum score:
- **`ops_observability`** (D5): session logging (JSONL), OpenTelemetry traces, git attribution, PR templates. Node `ops-observability` (currently stub/near-term).
- **`ops_cost`** (D5): KV-cache economics (10× cached/uncached), 100:1 input ratio, 3–10× multi-agent cost, cache TTL. Node `ops-cost` (stub/near-term, cross-cutting). Cross-links `ctx-assembly` (KV-cache) + `multi_agent` (the 3–10× multi-agent cost figure now has a strict-live neighbor).
- **`ops_hitl`** (D5): approval gates on irreversibility, planning-only modes, branch protection, confidence calibration, escalation. Node `ops-hitl` (stub/near-term). Cross-links `env-guardrails`.

These three complete the D5 Production Operations spine (alongside the already-strict-live `ops_security`). After Wave 6, a **20-wide** merge. Still deferred (intentional, no active book-consumer): `ops-sandboxes` (OS-isolation infra), `eval-tooling-landscape`, `eval-benchmarks`, `field-adoption`, `field-market`. The Wave-6 pick is editorial — confirm with the user only if the depth-gate is unclear; otherwise run straight through.

## Per-dossier recipe + tooling (unchanged, proven 16×)

Recipe: `2026-05-26_wave1-progress-handoff.md` §"battle-tested per-dossier recipe" (8 steps). Tooling: the toolkit's `scripts/build_excerpt_anchor.py` (v2.5.0, self-verifying); `~/Claude/_merge_tracks.py` (within-project); `WAVE=wave6 MERGE_DATE=… ~/Claude/_merge_projects.py` (cross-project). Toolkit venv: `~/Claude/research_toolkit/.venv/bin/python`. Validators take a **file** path; `verify_citations`/`build_dashboard`/`build_claim_graph` take the **dir**.

## Follow-ups / open items (none block Wave 6)

- **`sub_agents` SPA live↔cache drift flag (still pending).** `claude.com/blog` is a client-rendered SPA whose live DOM diverged from the cached snapshot at gather time; the anchored excerpt is verbatim to the **cache** (source of truth), the CoVe FLAG records the drift. Recheck whether the live SPA content stabilizes (or moves to a static URL); if it diverges materially, mint a corrected atom. No book claim is at risk — the cache anchor is sound.
- **MCP RC recheck (auto-flagged, still pending — 2026-07-28).** The 5 `mcp_design` "coming" atoms (`stale_after_days: 62`, Wave 4) go stale **exactly on 2026-07-28**; `/freshness-audit` will surface them then. Backlog item `mcp-rc-2026-final-deep-pass` covers the MCP Apps / ext-tasks / Extensions deep pass + the D5 supersede. **Not yet due.**
- **Harness-engineering vocabulary still settling (backlog `harness-engineering-vocabulary-settling`).** The provenance arc is dated history; recheck the term landscape as it crystallizes — a cleaner T1 frame statement may supersede the provenance atoms.
- **Anthropic-vs-Cognition multi-agent tension (backlog `anthropic-vs-cognition-multiagent-tension`).** Deliberately unresolved; revisit only if a tie-breaking primary or measured comparison lands. Present both in the book.
- **`twelve-factor-agents` + `deep-agents-pattern` + `agentic-engineering-vibe-coding` (backlog).** Their parents (harness-frame / agent-multipatterns) went strict-live, but these specific sub-topics stayed out of the dossiers' anchored scope (12-Factor list/40%-threshold; LangChain deep-agents; Karpathy floor-vs-ceiling thesis). Notes added; promote by gathering the respective primaries (D1 — mint new atoms).
- **`advanced-tool-use-effect-sizes` (backlog, Wave 4).** The escalated Anthropic figures (~85% token reduction; 79.5→88.1) — gather that post as a primary to anchor them verbatim. Unchanged.
- **Mem0 volatile recheck (Wave 2).** Rechecks **after 2026-06-26** — not yet due.
- **Toolkit burn-in.** Log Wave-5 lessons to `~/Claude/research_toolkit` `BURN_IN_NOTES.md` — the headlines are the **provenance-as-dated-history** family pattern, the **tension-preservation** discipline, and the **SPA live↔cache drift** convention.

## Tasks

Wave 5 (4 D1 dossiers + orphan bridge + 17-wide merge + in-repo bridge + handoff) **all completed except the parent commit + memory + toolkit burn-in**. Bridge edits staged on `research-program-v0` (uncommitted by design — parent commits after review). Commits stay local (branch is ahead of origin across all five waves; push only on request). Wave 6 (Production Operations, D5) not yet started.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
