# Part I v1.0 prose round — wrap-up

**Date completed**: 2026-05-25
**Plan reference**: `~/.claude/plans/did-we-do-to-async-lark.md` (Part I v1.0 prose — Ch 1-4 + Part I summary)
**Branch**: `part-i-v1.0-prose` — held for human review before merge to `main`, per `AUTHORS.md` ("no autonomous chapter-prose merges before human review")
**Session**: one sustained session, Phases 0-4.

## What completed (acceptance gates)

- **Phase 0** — scaffold v4.2.0 → v4.3.0 (pin + lockfile); `build:tips` wired into `predev`/`prebuild`; `/tips` route deferred (workspace-hoisting bug). Commit `ce20880`.
- **Phase 1** — `ch01-principles.mdx` rewritten to spec as the Part I template + 4 supplements refined to v1.0; 3 T1 citations via `<Citation>`; numbered Tips 1-2; two-tier Exercises/Practice. Commit `de2415b`.
- **Phase 2** — Ch 2 / Ch 3 / Ch 4 by sequential sub-agents (one per chapter), each a canonical chapter + 4 supplements; Tips 3-8; manifest grew to 11 sources. Commits `a38a4b2` / `3032d6a` / `9cb98f6`.
- **Phase 3** — `part1-summary.mdx` promoted to all-real (Ch 2-4 placeholders replaced; supplements linked). Commit `551b59e`.
- **Phase 4** — OUTLINE Ch 1-4 marked ✅ shipped + PoC-pointer rows; style-guide §3 citation mechanism corrected; COMPARISON Round 4; this wrap-up. (synthesis commit)
- Build clean throughout; each chapter renders the full component set (YouWillLearn / Figure / WorkedExample / Pitfall / Tip / Exercise / Practice / Citation); Tip registry 1-8 extracts via `build-tips`; per-chapter build-verified before each commit.

## Gate deltas vs the plan

- **Citations**: plan's "≥5 T1 per chapter" → cite-where-warranted (user decision); 2-4 genuine T1 per chapter. All PROVISIONAL pending human source-verification.
- **Page count**: plan predicted 31 → ~35; the four chapter routes already existed (dynamic `/chapters` route), so the new content is per-chapter + 12 new PoC routes; validator reports "34 chapter(s) checked" (content entries, not routes).
- **Parallel → sequential agents** (see Friction).

## Friction + surprises

- **Citation mechanism**: plan assumed `<Cite key>` + manifest→references.json via `build:bib`. Ground truth — `<Cite>` reads a BibTeX `.bib` (absent here); the tools-profile citation is `<Citation src>` reading the `sources` collection from `manifest.yaml`. Switched to `<Citation>`; corrected style-guide §3.
- **Frontmatter mismatch**: `toolsChapterSchema` requires `last_verified` and lacks `cert_domains` / `introduced_in_version` / `last_updated`. Kept both (schema field + convention fields; the latter stripped by non-strict Zod). Logged as a gap.
- **v4.3.0 `/tips` route** hard-fails the build under npm-workspace hoisting (hardcoded `../../../src/data/tips.json`). Route deferred; `<Tip>` still renders inline.
- **`build-tips`** found 0 tips by default (`readChaptersBase` couldn't resolve the base from the `defineBookSchemas()` spread); fixed via `BOOK_CHAPTERS_DIR` in the script.
- **`/chapters` route conflict**: v4.3.0 auto-injects `/chapters/[...slug]`, conflicting with the consumer's hand-written route (which uses the richer `Chapter` layout vs the scaffold's `Base`). Benign WARN; status quo kept.
- **Parallel → sequential agents**: the plan's 3 parallel agents would race on the shared `manifest.yaml` + build output; ran sequentially instead.
- **Tool-wrapper artifact**: the Ch 3 agent injected stray `</content>`/`</invoke>` tags that broke the MDX parse; it self-fixed, and a full scan of all chapter/PoC files confirmed clean.

## Open follow-ups

- **HITL citation verification** (`AUTHORS.md`): the 11 manifest sources are provisional — verify URLs + publish dates against primaries before promoting chapters to `validated`.
- **4 scaffold gaps to file upstream** (HITL): `/tips` workspace-hoisting; `build-tips` dir resolution; `/chapters` route conflict; frontmatter convention vs schema. All logged in `docs/scaffold-gaps.md` with suggested fixes.
- **Cross-reference harmonization**: forward-refs are prose (no `<XRef>`, since target chapters don't exist yet). Ch 2 kept them number-free; Ch 1/3/4 name numbers. A future XRef-linking pass (once Part II+ chapters exist) should harmonize the style and convert to `<XRef>`.
- **Frontmatter convention**: reconcile CLAUDE.md (last_updated / cert_domains / introduced_in_version) with the scaffold schema (last_verified) — scaffold adds the fields, or CLAUDE.md aligns to `last_verified`.
- **`/tips` route + cross-volume `defineTips`**: revisit once the upstream workspace-hoisting fix ships.

## Handoff hook

Part I (Ch 1-4 + summary) is v1.0 on branch `part-i-v1.0-prose` (6 content commits + 1 synthesis), awaiting human review before merge to `main`. Next natural round: **Part II prose (Ch 5-8)** — the PoCs already exist (Rounds 2-3); promote them to v1.0 chapters mirroring the Part I template, continuing the Tip registry from 9, and reusing the now-settled `<Citation>` + frontmatter + two-tier-exercise patterns.
