# Scaffold gaps log

Running log of gaps in [`@brandon_m_behring/book-scaffold-astro`](https://github.com/brandon-behring/book-scaffold-astro) discovered while porting books in this repo. Batched as upstream issues / PRs at phase boundaries (Phase 0, 1, 2, ...).

## Format

Each row: `phase | chapter | LaTeX construct or scenario | what's missing in scaffold | suggested improvement | filed?`

Empty log is fine — the file exists from Phase 0 to frame the dogfooding practice from day one.

## Gaps

| Phase | Chapter | Scenario | What's missing | Suggested improvement | Filed? |
|---|---|---|---|---|---|
| 0 | (pre-port, discovery) | `\official{X}` / `\practitioner{X}` / `\convergence{X}` inline tags in LaTeX source. Scaffold's `<Tag kind="...">` covers this exactly (same kinds, same labels, same Warm Tol palette), but `LATEX_TO_MDX_MAPPING.md` only lists `<Tag>` under "volatility badge" / "enum chip in chapter meta" — the practice-tag mapping isn't documented. | Discoverability gap (component exists, mapping wasn't surfaced). The scaffold's `<Tag>` source comment says it maps `\tagofficial{}` etc., but the LaTeX source actually uses `\official{}`/`\practitioner{}`/`\convergence{}`. | Update `LATEX_TO_MDX_MAPPING.md` to list `<Tag kind="official\|practitioner\|convergence">` as the canonical map for inline practice tags, naming both the `\tag*` and `\official`/`\practitioner`/`\convergence` LaTeX-side variants. | ✅ [#29](https://github.com/brandon-behring/book-scaffold-astro/issues/29) (2026-05-22) |
| 0 | ch01-principles | `<MarginNote>` actual API is `variant: "note"\|"warning"\|"tip"` + optional `label`, NOT a generic `title` prop. The `LATEX_TO_MDX_MAPPING.md` row for MarginNote has an empty Props column. Consumer guessing leads to either broken JSX or visual inconsistency. | Documentation gap: row in mapping table doesn't surface the props. Worth promoting since margin notes are *the* dominant LaTeX construct in any Tufte-style book. | Fill in MarginNote's Props column in `LATEX_TO_MDX_MAPPING.md` with the `variant` + `label` signature and the LaTeX-side mapping: `\marginnote[L]{}` → `variant="note" label="L"`, `\margintip{}` → `variant="tip"`, `\marginwarning{}` → `variant="warning"`. | ✅ [#30](https://github.com/brandon-behring/book-scaffold-astro/issues/30) (2026-05-22) |
| 0 | ch01-principles | `<MarginNote>` source comment says "25-word cap is enforced in the validator (Phase 2.6); not enforced here at runtime." The user's uncommitted lint_margin_notes.py work in `~/claude-best-practices/` does exactly this enforcement at the LaTeX level. Could feed directly into the scaffold's planned validator. | Cross-repo opportunity: the lint_margin_notes.py + lint_practice_tags.py work already prototyped against LaTeX could PR upstream as the scaffold's MDX validator. | Once Phase 2.6 validator design lands, offer to port lint_margin_notes.py + lint_practice_tags.py logic as the MDX-side checker (or as a contributed example). | Pending (separate; not Phase 0 critical) |
| 0 | ch01-principles | `<XRef>` JSDoc comment says "An unknown id renders an inline '[?label]' placeholder so the build doesn't fail before we have chapters with labels. Once Phase 2.6 lands, the validator promotes 'unknown label' from a warning to a build error." Phase 2.6 has already shipped — the validator DOES block builds on unknown ids — so the JSDoc is stale and ambiguous about runtime behavior. | Documentation drift: the comment in `components/XRef.astro` should drop the "doesn't fail" wording and clearly state the validator blocks builds for unknown ids in the current release. | Update XRef.astro JSDoc. Also worth a brief CONTRIBUTING note about bootstrapping a fresh book — early chapters that reference yet-to-be-ported chapters need plain-prose substitutes (or `{/* commented out */}` XRefs) until the targets exist. | ✅ [#31](https://github.com/brandon-behring/book-scaffold-astro/issues/31) (2026-05-22) |
| 0 | ch01-principles | `build-scaffold validate` output prints `profile=minimal` even though the consumer's `.env` declares `BOOK_PROFILE=tools`. Unclear whether validator profile detection is broken or whether the validator intentionally runs in minimal mode for the chapter linting pass. | Worth confirming: should validate read `BOOK_PROFILE` from `.env` like `astro.config.mjs` does, or is the minimal-profile validation a deliberate baseline? If deliberate, the log line is misleading — say "(running baseline checks)" not "(profile=minimal)". | Investigate `book-scaffold validate` profile resolution; either fix to read from `.env`, or relabel the log line so consumers don't think their profile config is broken. | ✅ confirmed on existing [#20](https://github.com/brandon-behring/book-scaffold-astro/issues/20) (2026-05-22) |
| 0 | (entire bootstrap) | **`create-book` CLI does not scaffold `src/pages/`.** No `index.astro`, no `chapters/[...slug].astro`. A fresh book has no way to view its starter chapter until the consumer manually adds these routes. The scaffold's auto-injected pages (`/chapters`, `/print`, `/search`, `/references`, `/convergence`) build but no individual chapter is rendered. Discovered when running `npm run build` showed "5 page(s) built" with no per-chapter HTML. | The fixtures at `package/tests/visual/fixture*/src/pages/` already show the canonical pattern — copy those into create-book's scaffolded files. | Have `create-book` emit `src/pages/index.astro` and `src/pages/chapters/[...slug].astro` as part of the standard 11+ files, based on the chosen profile's fixture pattern. (Tested by: a fresh `npx create-book new-book && cd new-book && npm install && npm run build` should produce HTML for the starter chapter.) | ✅ [#28](https://github.com/brandon-behring/book-scaffold-astro/issues/28) HIGH (2026-05-22) |
| 0.7 | (PoC content types) | Per-PoC-kind layout selector missing. All custom content collections (e.g., `poc`, future `how-to`) currently render with the same shared layout. A cheat-sheet wants different typography/spacing than a tutorial; without per-kind layout swap, the visual differentiation between content types is undercut. | Build a `<PocLayout kind="tutorial|how-to|tldr|part-summary|cheat-sheet">` page-shell component that swaps typography/spacing/line-width per kind. | Add `<PocLayout>` to the scaffold component library; document the registration pattern in PACKAGE_DESIGN.md. | ✅ shipped in [v4.1.0](https://github.com/brandon-behring/book-scaffold-astro/releases/tag/v4.1.0) (closes [#56](https://github.com/brandon-behring/book-scaffold-astro/issues/56)) — adopted in `handbook/src/pages/poc/[...slug].astro` 2026-05-23 |
| 0.7 | (PoC content types) | `<WorkedExample>` collapsible component needed per PEDAGOGY.md two-level-disclosure rule. Worked examples should render collapsed-by-default with named anchor for direct linking. | Build `<WorkedExample title="...">` with `<details>`/`<summary>` semantics. | Add to scaffold component library. | ✅ shipped in [v4.1.0](https://github.com/brandon-behring/book-scaffold-astro/releases/tag/v4.1.0) (closes [#57](https://github.com/brandon-behring/book-scaffold-astro/issues/57)) — anchor pattern `#worked-example-{id}` |
| 0.7 | (PoC content types) | `<Pitfall>` callout variant — distinct visual signature from generic Warning. Per React.dev's rich callout vocabulary. | Build as a separate scaffold component (rose-tinted border; red-pill "Common mistake" label). | Add to scaffold component library alongside `<KeyIdea>` / `<TryThis>` / `<InsightBox>`. | ✅ shipped in [v4.1.0](https://github.com/brandon-behring/book-scaffold-astro/releases/tag/v4.1.0) (closes [#58](https://github.com/brandon-behring/book-scaffold-astro/issues/58)) — crimson border + `--warm-crimson` token |
| 0.7 | (PoC content types) | `<YouWillLearn>` chapter-opener callout — per React.dev, a first-class "what this chapter delivers" callout, visually distinct from generic blockquote. | Build with slotted bulleted body of learning outcomes; distinctive border + leading icon. | Add to scaffold component library. | ✅ shipped in [v4.1.0](https://github.com/brandon-behring/book-scaffold-astro/releases/tag/v4.1.0) (closes [#59](https://github.com/brandon-behring/book-scaffold-astro/issues/59)) — gold border + optional `prerequisites` prop |
| 1 | (build) | Empty `sources/manifest.yaml` produces noisy `[file-loader] No items found in sources/manifest.yaml` WARN on every build. Once a single bib entry is added the warning goes away, but for pre-bibliography projects it's confusing noise. | Suppress the WARN when `manifest.yaml` exists but has zero entries (valid pre-bibliography state). Reserve WARN for malformed manifests. | Update the scaffold's bibliography loader. | ✅ shipped in [v4.1.0](https://github.com/brandon-behring/book-scaffold-astro/releases/tag/v4.1.0) (closes [#60](https://github.com/brandon-behring/book-scaffold-astro/issues/60)) — collection skipped entirely for empty/whitespace/`[]` cases |
| 1 | (consumer collection schemas) | Custom collection schemas using `z.string()` for date fields fail with `Expected type 'string', received 'object'` because YAML auto-parses unquoted `YYYY-MM-DD` as a Date object. The scaffold's existing `chapters` collection works because its schema uses a different shape (possibly `z.coerce.date()`). | Document the gotcha: when defining a custom collection with date-string fields, either use `z.coerce.date()` or quote date values in MDX frontmatter (`last_updated: "2026-05-23"`). | Add a note to PACKAGE_DESIGN.md § "Custom collections" + a worked example. | ✅ shipped in [v4.1.0](https://github.com/brandon-behring/book-scaffold-astro/releases/tag/v4.1.0) (closes [#61](https://github.com/brandon-behring/book-scaffold-astro/issues/61)) — PACKAGE_DESIGN.md §5a documents both safe patterns |
| 1 (chapter prose blockers) | (PEDAGOGY decision #18) | No scaffold component for numbered Tips à la Pragmatic Programmer. Decision #18 committed (2026-05-25) to ~50 numbered Tips across 3 volumes; pull-out tip card at end of each volume; cross-volume registry. `<MarginNote variant="tip">` doesn't carry numbering or cross-volume coordination. | `<Tip n="14" title="Care About Your Craft">...</Tip>` + `defineTips({ volumeOffset: 0 })` + auto-generated `/tips` route per volume + `<TipsCard>` for the pull-out card. | Add to scaffold component library + per-book config + route-emitter. | ✅ shipped [v4.3.0](https://github.com/brandon-behring/book-scaffold-astro/releases/tag/v4.3.0) (closes [#70](https://github.com/brandon-behring/book-scaffold-astro/issues/70)) 2026-05-25 — `<Tip>` / `<TipsCard>` / `defineTips` shipped + adopted; standalone `/tips` route deferred under workspace hoisting (see row below) |
| 1 (chapter prose blockers) | (PEDAGOGY decision #19) | No scaffold component for two-tier exercise model per CS:APP. Decision #19 committed (2026-05-25) to inline `<Exercise>` at point of concept introduction + end-of-chapter `<Practice>` (difficulty 1-4) + `<ExerciseSolutions>` collecting solutions by id at chapter end. Existing `<TryThis>` covers single-tier inline only. | `<Exercise id="...">` + `<Practice id="..." difficulty="1-4">` + `<ExerciseSolutions />` block-component. | Add three components to scaffold library; difficulty markers via ◆/◇; auto-collect solutions by id. | ✅ shipped [v4.3.0](https://github.com/brandon-behring/book-scaffold-astro/releases/tag/v4.3.0) (closes [#71](https://github.com/brandon-behring/book-scaffold-astro/issues/71)) 2026-05-25 — `<Exercise>` / `<Solution>` / `<ExerciseSolutions>` / `<Practice>` shipped + adopted in Ch 1 (manual id↔for pairing; auto-collection deferred to v4.4.0+) |
| 0 (v4.3.0 adoption) | (build; all chapters) | **Auto-injected `/tips` route hard-fails the build under npm-workspace hoisting.** `routes: { tips: true }` injects the package's `pages/tips.astro`, which does `await import('../../../src/data/tips.json')`. That relative path assumes the package sits at `<consumer>/node_modules/...`; under a workspace it is hoisted to the **repo-root** `node_modules`, so the path resolves to `<repo-root>/src/data/tips.json` (nonexistent) instead of `handbook/src/data/tips.json`. Vite fails the import at bundle time, so the page's runtime try/catch "graceful skip" never fires. | Hardcoded `../../../src/data/...` import is fragile to `node_modules` depth and bypasses Astro/Vite path resolution. | Resolve the consumer's `src/data/tips.json` via a Vite alias / virtual module, or read it at integration time and pass via props — anything that doesn't assume a fixed `node_modules` depth. Until fixed, workspace consumers keep `<Tip>` inline (renders fine) and skip the `/tips` route. | ⏳ to file (HITL per AUTHORS.md) — logged 2026-05-25; route deferred in `handbook/astro.config.mjs` |
| 0 (v4.3.0 adoption) | (build:tips; all chapters) | **`book-scaffold build-tips` finds 0 tips by default in a workspace consumer.** `readChaptersBase` parses `content.config.ts` for the chapters loader's `base:`, but when the chapters collection comes from the `defineBookSchemas()` spread (no explicit `base:` literal in the consumer file), detection fails and the default scan misses `src/content/chapters` — so `<Tip>` instances aren't extracted into `tips.json`. Setting `BOOK_CHAPTERS_DIR=src/content/chapters` fixes it. | `readChaptersBase` can't see the base when chapters come from the schema-helper spread; the no-match fallback doesn't default to `src/content/chapters`. | Default to `src/content/chapters` when no base literal is found, or detect the `defineBookSchemas()`-spread case. | ⏳ to file (HITL) — worked around via `BOOK_CHAPTERS_DIR` in handbook `build:tips` script 2026-05-25 |
| 0 (v4.3.0 adoption) | (build; all chapters) | **v4.3.0 `/chapters/[...slug]` auto-injection conflicts with a hand-written chapter route.** The tools profile defaults `routes.chapters: true`; v4.3.0 (#69) now injects `/chapters/[...slug]` alongside the `/chapters` index. A consumer that hand-wrote `src/pages/chapters/[...slug].astro` (here, to use the richer `Chapter` layout — the scaffold's injected route gives tools chapters the lighter `Base` layout) gets a build WARN per chapter ("conflicts with higher priority route"). `chaptersSlug` is injected whenever `chapters` is true (not independently gateable), and disabling `routes.chapters` would also drop the `/chapters` index. | No way to keep the scaffold `/chapters` index while opting out of the injected `[...slug]` route; the injected route hardcodes the `Base` layout for tools with no per-consumer override. | Make `chaptersSlug` independently gateable (`routes: { chaptersSlug: false }`), and/or skip injection when the consumer ships its own route, and/or let consumers pick the layout for the injected route. | ⏳ to file (HITL) — benign WARN; consumer route renders correctly (Chapter layout); status quo kept 2026-05-25 |
| 1 (chapter prose) | ch01-principles | **Handbook frontmatter convention diverges from `toolsChapterSchema`.** CLAUDE.md + OUTLINE mandate `cert_domains`, `introduced_in_version`, and `last_updated`; `toolsChapterSchema` models none of these and instead requires `last_verified` (which drives the freshness banner via `getFreshness`). Non-strict Zod silently strips the convention fields, so they're invisible to the renderer + any cert tooling. | Schema doesn't model the cert/versioning fields a cert-aligned book needs; the freshness field name differs (`last_verified` vs the convention's `last_updated`). | Add optional `cert_domains` + `introduced_in_version`, and accept `last_updated` as an alias for `last_verified` (or document `last_verified` as canonical so the handbook convention + CLAUDE.md can align). | ⏳ to file (HITL) — ch01 carries `last_verified` (functional) + the convention fields (stripped) 2026-05-25; revisit CLAUDE.md frontmatter convention |
