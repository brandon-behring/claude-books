# Node
node_modules/
npm-debug.log*

# Astro
dist/
.astro/

# Env
.env
.env.local
.env.*.local

# Build artifacts
**/dist-pdf/
**/.pagefind/

# Generated glossary copies — synced from the canonical glossary/terms/ by
# scripts/sync-glossary.mjs (predev/prebuild). Source of truth is glossary/terms/.
*/src/content/glossary/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json

# Per-Claude-Code
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Playwright MCP runtime
.playwright-mcp/

# Confidential — Anthropic cert exam guide (marked "Need to Know" on every page).
# Internal research aid only; not for public distribution. See plan + cert-tracking docs.
instructor_*.pdf

# Cross-machine continuity bundle — out-of-repo memory + plan-file transfer artifact.
# Restored into ~/.claude on clone; kept here (archive + extracted) as a local reference only.
claude-books-continuity.tar.gz
claude-books-continuity/

# Research-program launchpad seeds + critique (local scratch). Error-prone starting points,
# NOT authoritative — the strict-live program re-verifies everything against primaries. Kept out
# of the public CC-BY repo. Relocated under docs/research-program/launchpad/ in the 2026-06-01
# reorg (was loose at the repo root).
/docs/research-program/launchpad/
/docs/environment-engineering-context-assembly-critique.md

# Source-archive fetch cache — runtime artifacts, never published
agentic-coding/sources/cache/*
!agentic-coding/sources/cache/.gitkeep
