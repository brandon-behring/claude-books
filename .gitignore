# Node
node_modules/
npm-debug.log*

# Astro
dist/
.astro/

# Env
.env
.env.local
.env.*.local

# Build artifacts
**/dist-pdf/
**/.pagefind/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json

# Per-Claude-Code
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Playwright MCP runtime
.playwright-mcp/

# Confidential — Anthropic cert exam guide (marked "Need to Know" on every page).
# Internal research aid only; not for public distribution. See plan + cert-tracking docs.
instructor_*.pdf

# Research-program launchpad seeds + critique (local scratch). Error-prone starting
# points, NOT authoritative — the strict-live program re-verifies everything against
# primaries (these even seeded the folk coinages it later caught). Kept out of the
# public CC-BY repo; left untracked across Waves 1–3. Remove a line to commit one for provenance.
/Environment Engineering and Context Assembly_ A Practitioner's Deep Dive for Production AI Agents.md
/compass_artifact_wf-e6cb3af6-51f4-4003-8f12-e9d9ce007699_text_markdown.md
/docs/environment-engineering-context-assembly-critique.md
