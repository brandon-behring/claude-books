# sources/manifest.yaml — captured sources for The Claude Architect's Reference (cert-aligned, tools profile).
# Top-level YAML array of source objects (see the handbook's manifest for the entry shape).
# Cited inline via <Citation src="id" />; built into src/data/references.json by `book-scaffold build-bib`.
# Empty for now — citations land as the D1–D5 chapters are authored. Backbone: docs/research-program/.
[]
